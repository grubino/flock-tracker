[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto

# Coverage settings
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    -v

# Ignore warnings
filterwarnings =
    ignore::DeprecationWarning
